{
  "name": "mahesa-portfolio",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "vercel-build": "node build.js",
    "build": "node build.js",
    "dev": "npx serve public -p 3000"
  },
  "devDependencies": {
    "fs-extra": "^11.0.0"
  }
}
